# Node / frontend
node_modules/
dist/
.angular/
*.tsbuildinfo

# .NET
bin/
obj/
[Bb]in/
[Oo]bj/

# thick:start
# Rust / Tauri
target/
apps/desktop/src-tauri/target/
# thick:end

# Editors / OS
.DS_Store
Thumbs.db
.vs/
.idea/
*.user

# Local env
*.local
.env
.env.*
!.env.example

# EF Core SQLite dev database (created by EnsureCreated at runtime)
*.db
*.db-shm
*.db-wal

# Playwright
frontend/playwright-report/
frontend/test-results/
frontend/.playwright/

# Claude Code personal/local settings (never shared)
.claude/settings.local.json

# Implementation plans are working scaffolding, not shipped artifacts — never checked in
docs/superpowers/plans/

# SDD scratch workspace (ledger, briefs, review packages) — never committed
.superpowers/

# Hook firing telemetry — accumulates every session; never checked in
.claude/hook-firings.jsonl
