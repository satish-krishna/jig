One-liner: A single-line text field (Spartan `hlmInput`) — pair it with a `Label` inside a `Field`; set `invalid` to show the destructive ring.

```jsx
<Field>
  <Label htmlFor="email">Email</Label>
  <Input id="email" type="email" placeholder="you@example.com" />
</Field>
```
