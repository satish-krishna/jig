One-liner: A multi-line text field sharing the Input skin — use for descriptions, commit messages, notes.

```jsx
<Field>
  <Label htmlFor="notes">Notes</Label>
  <Textarea id="notes" rows={5} placeholder="What changed?" />
</Field>
```
